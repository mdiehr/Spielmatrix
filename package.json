{
  "name": "Spielmatrix",
  "repository": "https://github.com/mdiehr/Spielmatrix.git",
  "version":"0.2.0",
  "devDependencies": {
    "jshint": "latest",
    "browserify": "latest",
    "mocha": "latest",
    "rimraf": "^2.2.8"
  },
  "scripts": {
    "clean": "rimraf dist/*",
    "lint": "jshint . --exclude-path .jshintignore",
    "test": "mocha test/"
  }
}